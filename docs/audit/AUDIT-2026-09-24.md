# HoldOn security and quality audit, 2026-09-24

Scope: the whole repository at `pre-audit-baseline` (d33555a), audited read-only first, then
fixed on branch `audit/opus-5.5`. Nothing was pushed and no history was rewritten.

## Baseline

| Measure | Result |
| --- | --- |
| `make` (release, both slices) | succeeds, 0 warnings |
| `swift build` (debug) | succeeds, 0 warnings |
| `make test` | 3 tests, 0 failures |
| Binary | universal, x86_64 + arm64, 1 149 728 bytes |
| Signature | `com.holdon.HoldOn`, Team CTBG66KD5F, flags `0x10000` (hardened runtime) |
| Entitlements | none |

## Verifying the README's privacy promises

| Promise | Verdict | Evidence |
| --- | --- | --- |
| No network connections | holds | No networking framework is linked (`otool -L`: AppKit, ApplicationServices, Carbon, Combine, CoreFoundation, CoreGraphics, CoreServices, Foundation, ServiceManagement, SwiftUI only). No `URLSession`/`CFSocket`/`nw_*` symbols in the binary, no `http(s)://` string in it, no network API in the sources. The only URL is the System Settings pane. |
| Keystrokes are never logged | holds for keystrokes, wording too broad | One `NSLog` exists (`KeyGuard.swift:61`) and fires only when the tap cannot be created; it carries no key data. Nothing else logs. → HO-05 |
| Nothing is written to disk | wording contradicts the settings file | Only `UserDefaults` keys `protectionOn`, `delay`, `guardClose`, `scope`, `targets` are written. No `FileHandle`/`write(to:)` anywhere. → HO-05 |
| Every event passes through untouched | wording too broad | True for every event except the guarded shortcut, which is swallowed and replayed by design. → HO-05 |

## Measured cost of the work inside the tap callback

Per call, release build, warm:

| Work | Cost |
| --- | --- |
| ASCII-capable layout lookup + `UCKeyTranslate` | 0.4 µs |
| `NSWorkspace.frontmostApplication` | 0.1 µs |
| `resolvingSymlinksInPath()` on the frontmost executable | 7.0 µs |
| `resolvingSymlinksInPath()` per path target | 7.7 µs |
| Full match against a two-entry list | 26.0 µs |

The keyboard-layout lookup is cheap and needs no cache. The symlink resolution is filesystem
I/O and is the only blocking call on the path. → HO-03

## Findings

| ID | Where | Finding | Level | Fix risk | Plan | Status |
| --- | --- | --- | --- | --- | --- | --- |
| HO-01 | `Sources/Store.swift:117` | `delay` is read from preferences without validation. A corrupt or wrongly typed value yields `0` (the hold ends instantly, ⌘Q is effectively unguarded) or an arbitrarily large number (⌘Q is swallowed for minutes). | critical | low | Clamp to 0.3–3.0 on read and on write; keep the key, the type and the 1.0 default. | fixed, c1bd44f |
| HO-02 | `Sources/HoldOnApp.swift:88`, `KeyGuard.swift:39` | A dead tap is never noticed. The 5 s poll only checks Accessibility; `CGEvent.tapIsEnabled` is never read and `isRunning` is never used. If the tap dies without delivering `tapDisabledBy*`, the menu still shows the filled hand while nothing is guarded. | high | low | Check tap health on the existing tick, re-enable or restart. | fixed, e3ef691 |
| HO-03 | `Sources/Store.swift:96` | Filesystem I/O inside the tap callback: `resolvingSymlinksInPath()` runs for the frontmost app and for every path target on each guarded keystroke. A target on a stalled network or removable volume blocks the callback, delaying input and risking a tap timeout. | medium | low | Resolve target paths when the list changes, resolve the frontmost executable once per pid. | fixed, 6d4e8bd |
| HO-04 | `Sources/KeyGuard.swift:104` | After a hold fires, the key-up of *any* guarded key is dropped, including ⌘W when the ⌘W guard is off and its key-down was passed through. The app then sees a key-down with no key-up. | medium | low | Remember which key codes were actually swallowed during this ⌘ press. | fixed, a19a38e |
| HO-05 | `README.md` Privacy | Three promises are stated more broadly than the code delivers, see the table above. | medium | none | Make the wording exact. No behaviour change. | fixed, 961902f |
| HO-06 | `Sources/KeyGuard.swift:71` | `stop()` leaves `didFire` and `cmdDown` set. After pausing and resuming, or a permission cycle, while ⌘ is held, the next ⌘Q can be swallowed with nothing happening until ⌘ is released once. | medium | low | Reset the press state in `stop()`. | fixed, a19a38e |
| HO-07 | `README.md` Build | The note that Accessibility permission is keyed to the code signature, and that an ad-hoc build has to be re-approved after every rebuild, is no longer in the README. | low | none | Restore a short note. | fixed, 961902f |
| HO-08 | `Sources/KeyGuard.swift:39` | `isRunning` is dead code. | low | none | Becomes the health signal of HO-02. | fixed, e3ef691 |
| HO-09 | `Tests/` | Only the matching rules are tested. The hold decision, the shortcut lookup and the delay clamp cannot be reached from a test. | low | low | Extract pure functions without changing behaviour, then test them. | fixed, 9a0085e |
| HO-11 | `Sources/SettingsView.swift:133` | With "Only these apps" picked and an empty list nothing is guarded, yet the header showed a green dot and "Holding ⌘Q in 0 apps". Found while verifying against the owner's own settings, which are exactly in that state. | low | none | Say so, in amber, with what to do about it. | fixed, ecf5620 |
| HO-10 | `Sources/Store.swift:70` | `Target(running:)` falls back to the id `"?"` for a process with neither bundle id nor executable URL. Such an entry can never match anything. | low | low | Do not add an entry that cannot match. | fixed, 07afc91 |

All eleven were fixed on `audit/opus-5.5`, one commit each, with `make` and `make test` green
after every one. Nothing was deferred.

## Found by the review of the audit's own diff

A `/code-review` pass over `pre-audit-baseline...HEAD` found that three of the fixes were
incomplete or wrong. They are listed here with the same weight as the rest.

| ID | Where | Finding | Level | Status |
| --- | --- | --- | --- | --- |
| HO-12 | `KeyGuard.swift` | Releasing ⌘ before the letter dropped the record of keys still held, so their key-up reached an app that never saw the key-down. | low | fixed, 9fbe2a4 |
| HO-13 | `KeyGuard.swift` | A hold pending when the tap went quiet was never called off: the timer runs on the main loop, independent of the tap, so it fired and replayed ⌘Q for a key the user had released. The guard could cause the very quit it exists to prevent. | critical | fixed, 9fbe2a4 |
| HO-14 | `KeyGuard.swift` | The two tap re-enable paths left the press state stale, so the first ⌘Q after a tap outage could be swallowed with nothing happening. HO-06's reset had been added to `stop()` only. | high | fixed, 9fbe2a4 |
| HO-15 | `Store.swift` | Swift does not re-enter `didSet` for an assignment made inside it, so the clamped hold time was corrected in memory but never written back; the comment claimed the opposite. | medium | fixed, f5f99b7 |
| HO-16 | `Store.swift` | HO-03 moved the filesystem work rather than removing it: a cache miss still resolved inside the callback, and `Store.init` and every `add()` resolved on the main thread. | medium | fixed, f3c2aad |
| HO-17 | `MenuBar.swift`, `SettingsView.swift` | `isHealthy` was added but never read, so a tap that died still looked identical to a healthy one until the next tick. HO-02 and HO-08 were only half fixed. | medium | fixed, bf456b3 |
| HO-18 | several | Cleanups: `HoldTime.min`/`max` shadowing `Swift.min`/`max`, `Store.minDelay`/`maxDelay` pass-throughs, a target built twice in the picker, a test relying on an unsupported `NSRunningApplication()`. | low | fixed, 0adfc26 |

## Found by the second review, after those fixes

The second `/code-review` pass showed that HO-13 was still open and that HO-12's fix had
created a worse case of its own.

| ID | Where | Finding | Level | Status |
| --- | --- | --- | --- | --- |
| HO-19 | `KeyGuard.swift` | `fire()` never checked that the tap was alive. HO-13 called a pending hold off only from the disable event, which the silent case never sends, and from the 5 s tick, which a hold of at most 3 s always beats. A tap that died quietly still ended in a replayed ⌘Q for a key long released. | critical | fixed, 812eb74 |
| HO-20 | `KeyGuard.swift` | Keeping the held-back key codes across a ⌘ release meant auto-repeat key-downs went through while the key-up was still eaten: presses with no release. A key-down that goes through now settles the debt. | high | fixed, e900cd7 |
| HO-21 | `KeyGuard.swift` | Forgetting a press claimed ⌘ was up while it could still be held, which silently disabled the escape hatch of releasing ⌘ to call off a hold. The modifier state is read back from the hardware instead. | high | fixed, e900cd7 |
| HO-22 | `HoldOnApp.swift`, `Store.swift` | The caller had to know the tap's state machine, and the health flag was published on every tick whether or not it changed, redrawing the menu bar and the settings window every five seconds. | medium | fixed, a77a2b2 |
| HO-23 | `SettingsView.swift` | The picker duplicated the failable initialiser's rule and dismissed the sheet even when nothing was added. | low | fixed, f180a96 |
| HO-24 | `SettingsView.swift`, strings | The empty-list status line was ungrammatical. | low | fixed, e611c95 |
| HO-25 | this document | Two "found sound" claims described behaviour the fixes had changed. | low | fixed in place |

## Found by the third review, after those fixes

The third `/code-review` pass found that two of the second round's fixes had gone wrong in
their own way, and picked up several smaller things.

| ID | Where | Finding | Level | Status |
| --- | --- | --- | --- | --- |
| HO-26 | `KeyGuard.swift` | The key-up of the pending hold was held back unconditionally, even when its key-down had already gone through. Adding ⇧ mid-hold, or a change of front app, sends the key-down on and then the app is owed its key-up: without it the key reads as stuck. | high | fixed, 595fa4c |
| HO-27 | `KeyGuard.swift` | HO-20 kept the ledger across a tap outage, but the key-ups during an outage go straight to the app, so the surviving debt was charged to some later unrelated key-up and stuck that key down elsewhere. The debts are dropped on an outage. | high | fixed, 595fa4c |
| HO-28 | `KeyGuard.swift` | HO-19 checked the tap up to 250 ms before the replay was actually posted. A tap dying, or protection being paused, inside that window still ended in a replayed ⌘Q. | high | fixed, ef8a09e |
| HO-29 | `Store.swift` | An `.app` with no bundle identifier was stored as the bundle path, which never equals a running executable path, so the entry guarded nothing while the header counted it. | medium | fixed, c37ad92 |
| HO-30 | `SettingsView.swift` | A picker row for a process that could not become a target did nothing when clicked, with no entry and no message. | medium | fixed, 404515f |
| HO-31 | `SettingsView.swift` | The window height is fixed while the stalled-tap status line could wrap to three lines, pushing the last row out of sight exactly when it matters. | low | fixed, eccb98a |
| HO-32 | `KeyGuard.swift` | A tap that keeps failing to be created logged its diagnostic on every five second tick, thousands of lines a day, against a README that promises one. | low | fixed, eccb98a |
| HO-33 | `HoldOnApp.swift` | `stop()` ran its whole body on every tick while protection was paused, including a window-server call and building the overlay window that had never been shown. | low | fixed, eccb98a |
| HO-34 | `Store.swift` | Adding several files at once wrote the list and started a background resolution per file, and the resolution ran even when no target was a path. | low | fixed, c37ad92 |
| HO-35 | `README.md`, `CHANGELOG.md`, `KeyGuard.swift` | The README claimed no networking framework is linked at all, true only of direct links; the changelog's test count was stale; `PressLedger.isEmpty` existed only for tests. | low | fixed, cbbdb24 |

### Raised by the review and deliberately not changed

- **The modifier timeout cancels a hold in progress.** The reviewer would rather
  `tapDisabledByTimeout` re-enabled the tap and let the hold continue, since the overlay
  vanishing mid-fill is visible. The events during the disabled window bypassed the tap,
  though, and the key-up may be among them: continuing would leave a hold that nothing can
  call off, and it would fire. Losing a hold the user can repeat is the smaller harm.
- **The running process's path is no longer resolved.** Matching compares the executable path
  as the kernel reports it against both forms of the target's path, plus the binary name. A
  target stored as a real path, reached through a symlink with a different name, now relies on
  that name fallback. Resolving the process side again would put a filesystem lookup back on
  the keystroke path, which HO-03 and HO-16 exist to remove. Left as the documented trade-off
  in the owner's decision below.

## Checked and found sound

- **Tap disable events.** `tapDisabledByTimeout` and `tapDisabledByUserInput` re-enable the tap in the callback itself.
- **Replay loop.** The replayed keystroke carries a marker in `eventSourceUserData`, and both the key-down and key-up paths check it before doing anything, so the tap cannot re-enter itself.
- **Focus.** The HUD window is borderless, `ignoresMouseEvents`, ordered with `orderFrontRegardless` and never made key, so it cannot take focus or swallow input. `canJoinAllSpaces` and `fullScreenAuxiliary` make it correct over full-screen apps and on secondary displays.
- **Auto-repeat.** While ⌘ is held, every repeated key-down is held back. Once ⌘ is released
  the repeats are no longer a shortcut and go through, and the key-up goes with them, so the
  app is never left with presses it has no release for.
- **Releasing ⌘ before Q, or Q before ⌘.** Both call the hold off and hide the HUD. After a
  tap outage the modifier state is read back from the hardware, so releasing ⌘ still works as
  the escape hatch (HO-21).
- **Layout switching.** Shortcuts resolve through the ASCII-capable layout, so a Russian or Greek layout keeps ⌘Q guarded; the lookup is re-read per keystroke, so switching layouts mid-session is picked up at once. A missing layout falls back to the US key codes 12 and 13.
- **Permission revoked.** `AXIsProcessTrusted` is re-read every 5 s; the guard stops, the icon turns into a warning triangle and the settings window shows the Allow row.
- **Preferences robustness.** A corrupt `targets` blob, an unknown `scope` and unexpected types for the booleans all fall back to safe defaults. Only `delay` is unguarded (HO-01).
- **Concurrency.** The tap is attached to the main run loop and the callback asserts main-actor isolation; settings and the layout API are only touched there.
- **Repository hygiene.** No secrets, keys, tokens or personal paths in the tracked files or in any commit message. No session links in the history. Two commits carry a plain `Co-Authored-By: Claude` line, which is what was asked for.
- **Licensing.** `NOTICE` reproduces the SlowQuit MIT notice verbatim and ships inside the app bundle; every derived source file carries the dual SPDX header; the README states both licences.
- **Build and signing.** Hardened runtime is on, no entitlements are requested (the event tap needs none; Accessibility is granted through TCC), the universal binary is signed as one and verifies.

## Needs the owner's decision

1. **Version and release.** `main` has moved well past the published 1.0.0: the menu bar and the settings window were rewritten, and launch behaviour changed. The next release wants `CFBundleShortVersionString` 1.1.0, `CFBundleVersion` 2, a `v1.1.0` tag, fresh artifacts and a cask bump. I changed none of these, since the cask and the published DMG depend on them.
2. **Notarization.** Needs a Developer ID Application certificate in your keychain, signing with `--timestamp`, an App Store Connect API key or app-specific password stored with `xcrun notarytool store-credentials`, then `xcrun notarytool submit dist/HoldOn-<v>.dmg --keychain-profile <name> --wait` and `xcrun stapler staple`. It also means a Makefile change. Tell me to do it and I will; I have submitted nothing.
3. **Non-activating panels.** When a utility shows a panel that takes key focus without activating its app (Spotlight-like launchers), `frontmostApplication` still reports the app underneath. The HUD then names that app, while the replayed ⌘Q reaches whichever window holds key focus. Correct handling needs the key window's owner, which costs a window-list query per keystroke. Documented, not changed.
4. **Secure Input.** While a password field holds secure input, the session tap receives nothing, so ⌘Q is unguarded for that moment. This is a macOS guarantee and cannot be worked around; it deserves a line in the README if you want users to know.
5. **Symlinks and name matching for path targets.** `/usr/bin/java` deliberately matches any
   binary named `java`, and since the keystroke path no longer touches the filesystem, that
   name fallback is also what covers a target reached through a symlink whose name differs, or
   a symlink re-pointed after the list was built. Keep it, or narrow it and accept that those
   cases stop being guarded.

## Suggestions for later

- Show the tap's health in the menu, so "running but not guarding" can never look identical to "guarding".
- Tests for the preferences round-trip, which currently needs `UserDefaults.standard`; an injectable store would make it reachable.
- A `notarize` target once a Developer ID exists, and a `dist` guard that refuses to package when the Info.plist version has no matching tag.
- `make dist` is not byte-reproducible: `ditto` and `hdiutil` record timestamps, so every build has a new checksum. Always take the cask's sha256 from the artifact you upload.

## Twelve minutes by hand

Everything below needs a person at the keyboard; no test can reach it.

1. **Three apps, tap and hold.** In Safari, Terminal and Finder: tap ⌘Q, nothing happens;
   hold it, the app quits. Finder has no Quit, so expect nothing there either way.
2. **⌘W guard.** Turn it on in settings. In a browser, tap ⌘W and the tab stays, hold it and
   the tab closes. Turn it back off and one tap closes the tab again.
3. **Russian layout.** Switch to ЙЦУКЕН, type a sentence, then tap and hold ⌘Q in a guarded
   app. It must behave exactly as on the Latin layout.
4. **Excluded app.** Pick "Every app except these", add one app, confirm that app quits on a
   tap while another app still needs the hold.
5. **A program with no bundle.** Run a Java or Python window, add it from the running-apps
   list, and hold ⌘Q on it.
6. **Sleep and wake.** `pmset sleepnow`, wake, wait five seconds, then hold ⌘Q again. This is
   the path the new tap health check covers.
7. **A hold interrupted by the system.** Start holding ⌘Q in a guarded app and, while still
   holding, lock the screen with ⌃⌘Q. Unlock, release the key, and wait: nothing may quit
   afterwards. This is the case where the hold used to outlive the tap and fire on its own.
8. **Revoke and re-grant.** Untick HoldOn in System Settings → Privacy & Security →
   Accessibility. Within five seconds the menu bar icon becomes a warning triangle. Tick it
   again: the hand returns and the hold works without relaunching.
9. **Password field.** In any password prompt, press ⌘Q. It goes straight through; that is
   the documented secure-input limit, not a regression.
10. **Launch at login.** Turn it on in the menu, log out and back in. HoldOn appears in the
   menu bar and opens no window.
11. **Launchpad.** Click HoldOn in Launchpad while it runs: the settings window comes forward.
    To see the first-launch pointer again, run `defaults delete com.holdon.HoldOn welcomed`
    and relaunch.
